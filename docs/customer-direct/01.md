# Compatibility baseline — issue #1

The inventory records commit 046fe532b6d48e949a1fb5e6bd04a09094f8ef0e: 33 routes, six business models, default-model consumers and environment variable names only. Synthetic legacy fixtures capture HUF order totals, application-wide administration and unsafe seed/database-unavailable behavior. These fixtures document compatibility risks; they do not certify those fallbacks as safe.

Run `pnpm baseline:check` and `pnpm test`. `scripts/baseline-check.mjs --fixture <path> --report <path>` accepts synthetic input and writes a JSON report without connecting to a database. It rejects removed legacy routes/models until a deliberate migration map is introduced. Additions remain allowed. Pinned dependency versions preserve the installed baseline; Node 24.19.0 and pnpm 11.19.0 are the verified local runtime.

Verification: synthetic fixture assertions, invalid totals/real-address rejection, complete source route/model inventory, clean frozen installation and Next.js production build passed. No production data was read or written. UI does not change. Rollback removes this inventory/check tooling; no data migration is required.

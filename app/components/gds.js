'use client';

// Keep the GDS umbrella behind an explicit Next.js client boundary.
// Consumers use the upstream components directly; no local primitives or tokens.
export * from '@sovereignsquad/gds/client';
